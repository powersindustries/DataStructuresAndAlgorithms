#pragma once


class Recursion
{
public:

    // Takes in a number and returns the factorial of that number
    // Factorial example: 5! = 5*4*3*2*1
    int Factorial(int number);

    // The fibonacci sequence is where each number is the sum of the two preceding numbers that came before it
    // F(n) = F(n-1) + F(n-2)
    int FibonacciSequence(int number);

};
